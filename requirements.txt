# Billiam OS — Python Dependencies
# All FOSS. No proprietary AI dependencies.
# Install: pip install -r requirements.txt

# ── Core Runtime ─────────────────────────────────────────────────────────────
openai>=1.0.0                 # OpenAI-compatible client for llama.cpp API
pyyaml>=6.0                   # YAML configuration parsing

# ── Voice Pipeline ───────────────────────────────────────────────────────────
edge-tts>=7.0.0               # Text-to-Speech (British voice, needs internet)
faster-whisper>=1.0.0         # Local Speech-to-Text (faster-whisper CTranslate2)

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=7.0.0
pytest-cov>=4.0.0

# ── Development ──────────────────────────────────────────────────────────────
ruff>=0.3.0

# ── Optional ─────────────────────────────────────────────────────────────────
# espeak-ng      — Offline TTS fallback (system package: sudo pacman -S espeak-ng)
# webrtcvad      — Voice Activity Detection (pip install webrtcvad)
# alsa-utils     — Audio capture (system package: sudo pacman -S alsa-utils)
# pipewire       — Modern audio server (system package)
